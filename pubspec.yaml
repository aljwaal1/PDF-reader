name: pdf_reader
version: 0.1.0+1
publish_to: 'none'

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.3.2
  flutter_tts: ^4.2.3
  google_mlkit_translation: ^0.15.1
  pdfrx: ^2.4.7
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
